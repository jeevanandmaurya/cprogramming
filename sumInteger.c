#include <stdio.h>

int main()
{

    int a, b, sum;
    scanf("%d %d", &a, &b); //Taking Input from User
    sum = a + b; //Adding Integers
    printf("%d", sum); //Printing sum of integers

    return 0;
}